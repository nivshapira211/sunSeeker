# Backend API base URL. When set, register and login send requests to the backend (data persists in MongoDB).
VITE_API_BASE_URL=https://localhost:3000/api

# URL that starts the Google OAuth flow (e.g. your backend's /auth/google endpoint).
# When set, the Login button redirects here instead of showing a login page.
VITE_GOOGLE_OAUTH_URL=
